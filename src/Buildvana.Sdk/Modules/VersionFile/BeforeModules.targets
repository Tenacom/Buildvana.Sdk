<Project>

  <PropertyGroup>
    <UseVersionFile Condition="'$(UseVersionFile)' != 'false'">true</UseVersionFile>
  </PropertyGroup>

  <Import Project="$(MSBuildThisFileDirectory)BeforeModules.Core.targets"
          Condition="$(UseVersionFile)" />

</Project>
